using F7s.Modell.Handling;

namespace F7s.Modell.Abstract {
    public interface IGameValue {

        void ConfigureInfoblock (Infoblock infoblock);
        void ConfigureContextMenu (ContextMenu contextMenu);
    }
}
